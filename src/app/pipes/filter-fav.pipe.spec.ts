import { FilterFavPipe } from './filter-fav.pipe';

describe('FilterFavPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFavPipe();
    expect(pipe).toBeTruthy();
  });
});
